version: '3.4'



services:
   
  taxapp:
    image: taxapp
    container_name: taxapp
    
    extra_hosts: 
      - "host.docker.internal:host-gateway"
  
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 1401:1401
    

    
